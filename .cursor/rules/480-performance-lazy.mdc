---
description: "480: Performance lazy"
scopes: [chat, edit]
tags: [performance, lazy]
priority: 480
alwaysApply: false
globs: ["**/*.swift"]
---

# Performance
- Scrolling collections default to `LazyVStack / LazyHGrid`.
- Expensive shadow on lists? Wrap cell in `.drawingGroup()` or pre-render image.
- Avoid `.shadow(radius: >10)` inside repeating views.

